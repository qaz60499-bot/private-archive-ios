// Reserved for a future narrowed Three.js loader. The current Memory Aperture
// intentionally imports the shared `three` chunk so it can reuse the same package
// instance as ArchiveAtmosphere without duplicating renderer code.
export {}
